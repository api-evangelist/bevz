description: Forbidden
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: User is not authorized to access this resource with an explicit deny
